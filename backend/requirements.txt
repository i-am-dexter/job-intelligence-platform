fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy==2.0.36
alembic==1.14.0
psycopg2-binary==2.9.10
pydantic==2.10.4
pydantic-settings==2.7.0
python-multipart==0.0.20
pdfplumber==0.11.4
python-docx==1.1.2
httpx==0.28.1
apscheduler==3.10.4
pytest==8.3.4
pytest-mock==3.14.0

# Optional: only needed if LLM_PROVIDER=anthropic (paid). The free default (ollama) and the
# free-tier alternative (groq) both work over plain HTTP via httpx above, no extra SDK needed.
# anthropic==0.42.0
